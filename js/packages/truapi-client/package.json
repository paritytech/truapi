{
  "name": "@truapi/client",
  "version": "0.1.0",
  "description": "TrUAPI typed client: transport, frames, SCALE codecs",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "prepare": "npm run build",
    "codegen": "cargo run -p truapi-codegen -- --input ../../../target/doc/truapi.json --output src/generated",
    "test": "for f in test/*.test.mjs; do echo \"=== $f ===\" && bun \"$f\" || exit 1; done"
  },
  "devDependencies": {
    "typescript": "^5.7"
  },
  "dependencies": {
    "scale-ts": "^1.6.1"
  }
}
