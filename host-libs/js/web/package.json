{
  "name": "@parity/host-web",
  "version": "0.1.0",
  "description": "Browser TrUAPI host wrapper: iframe embedding and Web Worker-backed WASM provider",
  "license": "MIT",
  "author": "Parity Technologies <admin@parity.io>",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc",
    "prepare": "npm run build",
    "test": "for f in test/*.test.mjs; do echo \"=== $f ===\" && node --test \"$f\" || exit 1; done"
  },
  "dependencies": {
    "@parity/host-shared": "file:../shared",
    "@parity/truapi": "file:../../../js/packages/truapi"
  },
  "devDependencies": {
    "typescript": "^5.7"
  }
}
