# TrUAPI Explorer

Docs-only browser for the TrUAPI service surface. All data is sourced from the
codegen registry exposed by `@parity/truapi/explorer/versions`. Built with
Vite + React + Tailwind v4. Static SPA, dark-only.
